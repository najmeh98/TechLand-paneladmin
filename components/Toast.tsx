import { forwardRef, Ref } from "react";
import { ToasterRef } from "./types/toastr";

// export const Toast = forwardRef((ref: Ref<ToasterRef>, props) => {
//   return <div>{
//       ref.map((item , index) => {})
//     }</div>;
// });
